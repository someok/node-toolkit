{
  "version": "independent",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "command": {
    "publish": {
      "ignoreChanges": ["**/*.md", "**/*.txt", "**/__tests__/**"],
      "message": "chore(release): publish"
    },
    "version": {
      "conventionalCommits": true
    }
  }
}
