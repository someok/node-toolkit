const fs = require('fs');

/**
 * 根据给定 {@link Meta} 和 文件列表输出 epub 格式文件到给定目录。
 *
 * @param {Path} toFolder 目标目录
 * @param {Meta} meta 元数据
 * @param {Array} files 文件列表
 */
exports.outputEpubStructure = function(toFolder, meta, files) {};
